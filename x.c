#include <stdio.h>
int main(int argc, char const *argv[])
{
    int x,y;

    scanf("%d %d", &x, &y);

    int z = x + y;

    for (int x = 1; x <= count; i++)
    {
        /* code */
    }
    
    return 0;
}
